(executable
  ((name jymandra)
   (public_name jymandra)
   (package jupyter-imandra)
   (modes (byte))
   (libraries (jupyter-kernel containers unix
               compiler-libs.common compiler-libs.toplevel
               imandra.lib z3 tyxml))
  ))
