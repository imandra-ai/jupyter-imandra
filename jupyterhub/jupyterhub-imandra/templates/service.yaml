apiVersion: v1
kind: Service
metadata:
  name: {{ template "jupyterhub-imandra.fullname" . }}
  labels:
    app: {{ template "jupyterhub-imandra.name" . }}
    chart: {{ template "jupyterhub-imandra.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "jupyterhub-imandra.name" . }}
    release: {{ .Release.Name }}
